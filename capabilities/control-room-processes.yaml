naftiko: 1.0.0-alpha2
info:
  label: Robocorp Control Room API — Processes
  description: 'Robocorp Control Room API — Processes. 3 operations. Lead operation: List Processes. Self-contained Naftiko
    capability covering one Robocorp business surface.'
  tags:
  - Robocorp
  - Processes
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ROBOCORP_API_KEY: ROBOCORP_API_KEY
capability:
  consumes:
  - type: http
    namespace: control-room-processes
    baseUri: https://cloud.robocorp.com/api/v1
    description: Robocorp Control Room API — Processes business capability. Self-contained, no shared references.
    resources:
    - name: workspaces-workspace_id-processes
      path: /workspaces/{workspace_id}/processes
      operations:
      - name: listprocesses
        method: GET
        description: List Processes
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: limit
          in: query
          type: integer
        - name: cursor
          in: query
          type: string
      - name: createprocess
        method: POST
        description: Create Process
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: workspaces-workspace_id-processes-process_id
      path: /workspaces/{workspace_id}/processes/{process_id}
      operations:
      - name: getprocess
        method: GET
        description: Get Process
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.ROBOCORP_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: control-room-processes-rest
    port: 8080
    description: REST adapter for Robocorp Control Room API — Processes. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/workspaces/{workspace-id}/processes
      name: workspaces-workspace-id-processes
      description: REST surface for workspaces-workspace_id-processes.
      operations:
      - method: GET
        name: listprocesses
        description: List Processes
        call: control-room-processes.listprocesses
        with:
          limit: rest.limit
          cursor: rest.cursor
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createprocess
        description: Create Process
        call: control-room-processes.createprocess
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workspaces/{workspace-id}/processes/{process-id}
      name: workspaces-workspace-id-processes-process-id
      description: REST surface for workspaces-workspace_id-processes-process_id.
      operations:
      - method: GET
        name: getprocess
        description: Get Process
        call: control-room-processes.getprocess
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: control-room-processes-mcp
    port: 9090
    transport: http
    description: MCP adapter for Robocorp Control Room API — Processes. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: list-processes
      description: List Processes
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: control-room-processes.listprocesses
      with:
        limit: tools.limit
        cursor: tools.cursor
      outputParameters:
      - type: object
        mapping: $.
    - name: create-process
      description: Create Process
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: control-room-processes.createprocess
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-process
      description: Get Process
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: control-room-processes.getprocess
      outputParameters:
      - type: object
        mapping: $.
